# Plan: <short title>

## E2E harness

- Existing harness: yes/no (framework, config path, how to run)
- If no: the dev sets it up as part of this run (config at repo root, browsers/deps installable in a fresh worktree). QA only writes tests under `tests/e2e/`.

## Environment

- Install: `...`
- Run app / server for e2e: `...` (port, base URL, env vars)
- Unit tests: `...`
- Lint / typecheck: `...`

## Dependencies

- New dependencies this task needs: ... or "None". The reviewer blocks any dependency not listed here.

## Implementation steps

1. ... (files to create/modify, by path)
2. ...

## Unit tests to add

- ...

## Risks / things to watch

- ...
